interface CreateCardParams {
  frontText: string;
  backText: string;
  id: number;
}
export default function CreateCard({
  frontText,
  backText,
  id,
}: CreateCardParams) {
  return (
    <div>
      hiii
    </div>
  );
}

